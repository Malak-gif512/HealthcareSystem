namespace HealthcareSystem.Domain.Enums
{
    // Defines the system hierarchical roles for Access Control
    public enum UserRole
    {
        Admin = 1,
        Nurse = 2,
        Patient = 3
    }
}